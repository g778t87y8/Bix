# توثيق تطبيق Bix

## نظرة عامة

Bix هو تطبيق لمشاركة الفيديو مشابه لتطبيق TikTok، يتيح للمستخدمين مشاهدة ومشاركة مقاطع الفيديو القصيرة. يستخدم التطبيق Next.js وFirebase للمصادقة وتخزين البيانات.

## هيكل المشروع

```
Bix/
├── public/                  # الملفات الثابتة
├── src/
│   ├── app/                 # صفحات التطبيق (Next.js App Router)
│   │   ├── auth/            # صفحة المصادقة
│   │   ├── discover/        # صفحة اكتشاف المحتوى
│   │   ├── feed/            # صفحة التغذية الرئيسية
│   │   ├── inbox/           # صفحة الرسائل
│   │   ├── profile/         # صفحة الملف الشخصي
│   │   ├── upload/          # صفحة رفع الفيديو
│   │   ├── video/           # صفحة عرض فيديو محدد
│   │   ├── layout.tsx       # التخطيط العام للتطبيق
│   │   └── page.tsx         # الصفحة الرئيسية
│   ├── components/          # مكونات التطبيق
│   │   ├── auth/            # مكونات المصادقة
│   │   ├── layout/          # مكونات التخطيط
│   │   ├── upload/          # مكونات رفع الفيديو
│   │   └── video/           # مكونات عرض الفيديو
│   └── lib/                 # المكتبات والخدمات
│       ├── firebase.ts      # تكوين Firebase
│       ├── mockData.ts      # بيانات وهمية للعرض
│       └── services/        # خدمات التطبيق
├── server.js                # خادم مخصص
├── next.config.js           # تكوين Next.js
└── package.json             # تبعيات المشروع
```

## المكونات الرئيسية

### 1. نظام المصادقة

يدعم التطبيق ثلاثة أنواع من المصادقة:
- **تسجيل الدخول بالبريد الإلكتروني وكلمة المرور**: يستخدم Firebase Authentication
- **تسجيل الدخول بحساب Google**: يستخدم مزود المصادقة من Google
- **تسجيل الدخول كضيف**: يستخدم localStorage لتخزين بيانات المستخدم الضيف

المكونات الرئيسية:
- `AuthProvider.tsx`: يدير حالة المصادقة ويوفر معلومات المستخدم لجميع أنحاء التطبيق
- `AuthForm.tsx`: نموذج تسجيل الدخول والتسجيل

### 2. عرض الفيديو

يعرض التطبيق مقاطع الفيديو في واجهة تشبه TikTok مع إمكانية التمرير الرأسي.

المكونات الرئيسية:
- `VideoCard.tsx`: يعرض فيديو واحد مع معلوماته وأزرار التفاعل
- `feed/page.tsx`: يعرض قائمة الفيديوهات في واجهة التمرير

### 3. نظام الإشعارات

يوفر التطبيق نظام إشعارات لإعلام المستخدمين بالأحداث المختلفة.

المكونات الرئيسية:
- `ToastManager.tsx`: يدير حالة الإشعارات ويوفر واجهة برمجية لإظهار الإشعارات
- `ToastNotification.tsx`: يعرض إشعارًا واحدًا مع أنيميشن

## الخدمات

### Firebase

يستخدم التطبيق Firebase للخدمات التالية:
- **المصادقة**: تسجيل الدخول وإدارة المستخدمين
- **Firestore**: تخزين بيانات التطبيق (غير مستخدم حاليًا في النسخة التجريبية)
- **Storage**: تخزين ملفات الفيديو والصور (غير مستخدم حاليًا في النسخة التجريبية)

## البيانات الوهمية

في النسخة الحالية، يستخدم التطبيق بيانات وهمية من `mockData.ts` لعرض:
- قائمة الفيديوهات
- معلومات المستخدمين
- التعليقات

## تشغيل التطبيق

1. تثبيت التبعيات:
```bash
npm install
```

2. تشغيل الخادم المخصص:
```bash
node server.js
```

3. الوصول إلى التطبيق عبر:
https://work-1-ryuuqjomxbqjnxrs.prod-runtime.all-hands.dev

## التحسينات المستقبلية

1. ربط التطبيق بمشروع Firebase حقيقي
2. تنفيذ وظيفة رفع الفيديو
3. إضافة نظام تعليقات حقيقي
4. تحسين أداء تشغيل الفيديو
5. إضافة ميزات اجتماعية إضافية مثل المتابعة والإشعارات الحقيقية