# دليل استكشاف الأخطاء وإصلاحها

## مشاكل المصادقة

### لا يمكن تسجيل الدخول باستخدام البريد الإلكتروني وكلمة المرور

1. **الحل**: تأكد من أن بيانات اعتماد Firebase صحيحة في ملف `src/lib/firebase.ts`
2. **الحل**: تأكد من تمكين مزود المصادقة بالبريد الإلكتروني وكلمة المرور في لوحة تحكم Firebase
3. **الحل**: تحقق من سجلات وحدة التحكم في المتصفح للحصول على رسائل خطأ محددة

### لا يمكن تسجيل الدخول باستخدام Google

1. **الحل**: تأكد من تمكين مزود المصادقة من Google في لوحة تحكم Firebase
2. **الحل**: تأكد من أن النطاق المسموح به في إعدادات Firebase يتضمن نطاق موقعك
3. **الحل**: تحقق من سجلات وحدة التحكم في المتصفح للحصول على رسائل خطأ محددة

### مشاكل تسجيل الدخول كضيف

1. **الحل**: امسح ذاكرة التخزين المحلية للمتصفح وحاول مرة أخرى
2. **الحل**: تأكد من تمكين JavaScript في المتصفح
3. **الحل**: جرب متصفحًا آخر

## مشاكل عرض الفيديو

### الفيديوهات لا تعمل

1. **الحل**: تأكد من أن روابط الفيديو في `src/lib/mockData.ts` صالحة ويمكن الوصول إليها
2. **الحل**: تحقق من دعم المتصفح لتنسيق الفيديو المستخدم
3. **الحل**: تأكد من تمكين JavaScript في المتصفح

### مشاكل في التشغيل التلقائي للفيديو

1. **الحل**: تأكد من أن المتصفح يسمح بالتشغيل التلقائي للفيديو
2. **الحل**: انقر على الفيديو لتشغيله يدويًا
3. **الحل**: تحقق من سجلات وحدة التحكم في المتصفح للحصول على رسائل خطأ محددة

## مشاكل واجهة المستخدم

### الإشعارات لا تظهر

1. **الحل**: تأكد من تحميل مكون `ToastProvider` في التسلسل الهرمي للمكونات
2. **الحل**: تحقق من استدعاء `showToast` بالمعلمات الصحيحة
3. **الحل**: تحقق من سجلات وحدة التحكم في المتصفح للحصول على رسائل خطأ محددة

### مشاكل في التصميم المتجاوب

1. **الحل**: تأكد من استخدام المتصفح المحدث
2. **الحل**: جرب تحديث الصفحة
3. **الحل**: تحقق من حجم النافذة وتأكد من أنها ليست صغيرة جدًا

## مشاكل الخادم

### الخادم لا يعمل

1. **الحل**: تأكد من تثبيت جميع التبعيات باستخدام `npm install`
2. **الحل**: تحقق من سجلات الخادم في `server.log`
3. **الحل**: تأكد من أن المنفذ 12000 غير مستخدم بالفعل

### مشاكل في الوصول إلى التطبيق

1. **الحل**: تأكد من أن الخادم يعمل على العنوان `0.0.0.0` والمنفذ `12000`
2. **الحل**: تحقق من إعدادات جدار الحماية
3. **الحل**: استخدم عنوان URL الخارجي المقدم عند بدء تشغيل الخادم

## مشاكل أخرى

إذا واجهت مشاكل أخرى غير مذكورة هنا، يرجى اتباع الخطوات التالية:

1. تحقق من سجلات وحدة التحكم في المتصفح للحصول على رسائل خطأ
2. تحقق من سجلات الخادم في `server.log`
3. حاول مسح ذاكرة التخزين المؤقت للمتصفح وإعادة تحميل الصفحة
4. تأكد من تثبيت جميع التبعيات باستخدام `npm install`
5. أعد تشغيل الخادم باستخدام `node server.js`