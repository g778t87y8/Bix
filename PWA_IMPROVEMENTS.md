# تحسينات تطبيق الويب التقدمي (PWA)

تم تحويل تطبيق Bix إلى تطبيق ويب تقدمي (PWA) لتحسين تجربة المستخدم وإتاحة استخدام التطبيق حتى في حالة عدم الاتصال بالإنترنت.

## التحسينات المنفذة

### 1. إعداد Service Worker
- إنشاء ملف `sw.js` لتخزين الموارد الأساسية في ذاكرة التخزين المؤقت
- تنفيذ استراتيجيات مختلفة للتخزين المؤقت:
  - استراتيجية "الذاكرة المؤقتة أولاً" للموارد الثابتة
  - استراتيجية "الشبكة أولاً" لطلبات API
- تسجيل Service Worker عند تحميل التطبيق

### 2. إضافة ملف Manifest
- إنشاء ملف `manifest.json` لتعريف خصائص التطبيق
- تكوين الأيقونات والألوان وسلوك العرض
- دعم اللغة العربية والاتجاه من اليمين إلى اليسار

### 3. تحسين تجربة عدم الاتصال
- إنشاء صفحة "عدم الاتصال" مخصصة
- إضافة مكون `OfflineAlert` لإعلام المستخدم بحالة الاتصال
- تنفيذ آلية للكشف عن حالة الاتصال وإظهار الإشعارات المناسبة

### 4. تحسينات إضافية
- تخزين البيانات الأساسية للاستخدام في وضع عدم الاتصال
- تحسين أداء التطبيق من خلال تخزين الموارد المتكررة
- إضافة إمكانية تثبيت التطبيق على الشاشة الرئيسية

## كيفية استخدام التطبيق في وضع عدم الاتصال

1. قم بزيارة التطبيق مرة واحدة على الأقل أثناء الاتصال بالإنترنت
2. سيقوم Service Worker بتخزين الموارد الأساسية تلقائيًا
3. عند فقدان الاتصال، ستظهر إشعارات تنبيهية
4. يمكنك الاستمرار في تصفح المحتوى المخزن مسبقًا
5. عند استعادة الاتصال، سيتم تحديث المحتوى تلقائيًا

## تثبيت التطبيق على الأجهزة

### على أجهزة Android
1. افتح التطبيق في متصفح Chrome
2. انقر على قائمة النقاط الثلاث
3. اختر "إضافة إلى الشاشة الرئيسية"

### على أجهزة iOS
1. افتح التطبيق في متصفح Safari
2. انقر على زر المشاركة
3. اختر "إضافة إلى الشاشة الرئيسية"

## تحسينات مستقبلية

- تحسين تزامن البيانات عند استعادة الاتصال
- إضافة دعم للإشعارات الفورية (Push Notifications)
- تحسين استراتيجيات التخزين المؤقت للمحتوى الديناميكي
- إضافة دعم للمشاركة والتفاعل في وضع عدم الاتصال

## ملاحظات تقنية

- تم استخدام Cache API لتخزين الموارد
- تم تنفيذ استراتيجيات مختلفة للتخزين المؤقت حسب نوع الطلب
- تم استخدام navigator.onLine للكشف عن حالة الاتصال
- تم إضافة مستمعي أحداث 'online' و 'offline' لتحديث واجهة المستخدم